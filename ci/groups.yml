groups:
  - name: master
    jobs:
      - development
      - qa
  - name: pull-request
    jobs:
      - aws-azkaban-pr
  - name: admin-development
    jobs:
      - rotate-mysql-password-master-development
